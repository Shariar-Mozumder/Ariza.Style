mutation AddToCartItems($input:CartItemInput!){
    addItemToCart(input:$input){
        total,subtotal,has_shipping,items{cart_id,product_id,name,option{option_id,name,value,product_option_id}}
    }
}