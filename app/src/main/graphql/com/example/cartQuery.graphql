query CartQuery{
    cart{
        total,items{
            cart_id,product_id,name,quantity,
            option{
                name,
                product_option_value{
                    name}
            }
        }
    }
}